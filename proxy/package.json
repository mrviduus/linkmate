{
  "name": "linkmate-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "LinkMate inference proxy — holds the OpenAI key and enforces a per-user spend quota.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.84.0"
  }
}
